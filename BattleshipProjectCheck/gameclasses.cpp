/*
 * #include <gameclasses.hpp>

void GameBoard::displayBoard(std::vector<std::vector<char>> playingBoard)
{
    std::cout << "\n";
    for (int row = 0; row < game::rows; row++)
    {
        std::cout << "\t";
        for (int col = 0; col < game::columns; col++)
        {
            std::cout << playingBoard[row][col];
            std::cout << "\t";
        }
        std::cout << "\n";
    }
}
*/

